#include <iostream>
using namespace std;

int main()
{
	setlocale(0, "");
	cout << "обратный отсчет" << endl;
	int count;
	cout << "введи положительное целое число: ";
	cin >> count;
	while (count != 0) {
		cout << "отсчет: " << count << endl;
		count--;
	}
	return 0;
}
