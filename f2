#include <iostream>
using namespace std;
int main()
{
	setlocale(0, "");
	int mapsize, choice;
	char texture;
	cout << "фигура квадрат" << endl;
	cout << "[1] - пустой внутри(dead inside)" << endl;
	cout << "[2] - заполненный" << endl;
	cout << "выбери: ";
	cin >> choice;
	cout << "введи размер: ";
	cin >> mapsize;
	cout << "введи текстуру: ";
	cin >> texture;
	int b = mapsize / 2;
	switch (choice) {
	case 1: {
		for (int x = 0; x < mapsize; x++) {
			for (int y = 0; y < mapsize; y++) {
				if (x < mapsize / 2 and y < mapsize / 2) {
					cout << "\x1b[96m" << " " << texture << "\x1b[0m";
				}
				else if (x < mapsize / 2 and y >= mapsize / 2) {

					cout << "\x1b[93m" << " " << texture << "\x1b[0m";
				}
				else if (x >= mapsize / 2 and y < mapsize / 2) {
					cout << "\x1b[94m" << " " << texture << "\x1b[0m";
				}
				else if (x >= mapsize / 2 and y >= mapsize / 2) {
					cout << "\x1b[95m" << " " << texture << "\x1b[0m";
				}
			}
			cout << endl;
		}
	}
		break;
	case 2: {
		for (int x = 0; x < mapsize; x++) {
			for (int y = 0; y < mapsize; y++) {
				if (x < mapsize / 2 and y < mapsize / 2) {
					cout << "\x1b[96m" << " " << texture << "\x1b[0m";
				}
				else if (x < mapsize / 2 and y >= mapsize / 2) {

					cout << "\x1b[93m" << " " << texture << "\x1b[0m";
				}
				else if (x >= mapsize / 2 and y < mapsize / 2) {
					cout << "\x1b[94m" << " " << texture << "\x1b[0m";
				}
				else if (x >= mapsize / 2 and y >= mapsize / 2) {
					cout << "\x1b[95m" << " " << texture << "\x1b[0m";
				}
			}
			cout << endl;
		}
	}
	}
		return 0;
}
