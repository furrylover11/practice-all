#include <iostream>;
using namespace std;

int main() {
	int const a = 5;
	int arr[a]{ 4,2,6,8,9
	};
	for (int i = 0; i < a; i++) {
		for (int j = 0; j < a - 1; i++) {
			if (arr[j] < arr[j + 1]) {
				int buf = arr[j];
				arr[j] = arr[j + 1];
				arr[j + 1] = buf;
			}
		}
	}
}
